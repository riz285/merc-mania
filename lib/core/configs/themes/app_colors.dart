import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromARGB(255, 239, 83, 80);
  static const appBar = Color.fromARGB(255, 255, 193, 214);
  static const title = Colors.brown;
  /// Light mode colors
  static const lightTitle = Color.fromARGB(255, 211, 47, 47);
  static const lightText = Colors.pink;
  static const lightBackground = Colors.white;
  /// Dark mode colors
  static const darkTitle = Colors.blue;
  static const darkText = Color.fromARGB(255, 252, 228, 236);
  static const darkBackground = Colors.black;
}
